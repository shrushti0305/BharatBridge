{
  "name": "bharatbridge-fullstack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "cd server && node src/index.js",
    "dev": "npm run dev --prefix server"
  }
}
